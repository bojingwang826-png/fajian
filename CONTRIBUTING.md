# 参与贡献

感谢你愿意改进法笺。这个项目使用原生 HTML、CSS、JavaScript 和 Node.js，运行时不依赖第三方 npm 包。

## 开始之前

1. 在 Issues 中搜索是否已有相同问题。
2. 修复缺陷时，请提供可复现的最小输入和预期结果。
3. 涉及法律内容时，请标明资料来源、适用年份和法条版本。
4. 不要提交真实 API Key、个人题库、考试作答或受版权保护的完整资料。

## 本地开发

```bash
git clone https://github.com/bojingwang826-png/fajian.git
cd fajian
npm start
```

访问 `http://localhost:3737`。

运行测试：

```bash
npm test
```

## 提交建议

- 一个提交解决一个明确问题。
- 保持现有浅色、纸张感和墨绿色视觉语言。
- 修改文档解析器时，优先补充匿名化的最小测试样本。
- 修改判分逻辑时，验证满分、遗漏、部分命中和限定词冲突场景。
- 不要把模型生成结果作为测试中的唯一正确答案。

建议采用以下提交前缀：

- `feat:` 新功能
- `fix:` 缺陷修复
- `docs:` 文档更新
- `test:` 测试调整
- `refactor:` 不改变行为的代码整理

## Pull Request

Pull Request 请说明：

- 解决了什么问题；
- 用户可观察到的变化；
- 如何验证；
- 是否影响已有题库、判分结果或数据格式。

提交 Pull Request 即表示你有权贡献其中的代码和资料，并允许仓库所有者在本项目中使用这些内容。
